package com.coding.meet.todo_app.application

import android.app.Application // <<< --- PENTING: Tambahkan baris import ini!

class TodoApplication : Application() { // <<< --- PENTING: Tambahkan ": Application()" di sini!
    // Di sini Anda bisa menambahkan kode inisialisasi aplikasi Anda
    // Contoh: override fun onCreate() { super.onCreate() ... }
}